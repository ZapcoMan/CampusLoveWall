spring:
    application:
        name: SpringbootCampusLoveWall
    datasource:
        url: jdbc:mysql://localhost:3306/springboot_campus_love_wall?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true&allowPublicKeyRetrieval=true
        username: root
        password: 123456
        driver-class-name: com.mysql.jdbc.Driver
        type: com.alibaba.druid.pool.DruidDataSource
        druid:
          initial-size: 5
          max-active: 20

    data:
      redis:
        database: 0
        host: localhost
        port: 6379
        password:
        timeout: 10000
